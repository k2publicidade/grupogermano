'use client';

import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { CheckIcon, LockIcon } from '@/components/icons';

export default function SignupPage() {
  const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setLoading(true); setError('');
    const data = Object.fromEntries(new FormData(event.currentTarget).entries());
    const response = await fetch('/api/forms', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ form: 'cadastro', data }) });
    setLoading(false); if (response.ok) setSent(true); else setError('Não foi possível enviar agora. Tente novamente.');
  }
  return <section className="auth-page"><div className="auth-aside"><p className="section-note">PORTAL DO PARCEIRO</p><h1>Condições feitas para quem vende.</h1><p>Cadastre sua empresa para acessar preços por volume, disponibilidade e ferramentas de orçamento.</p><div className="auth-benefits"><span><CheckIcon /> Tabela comercial protegida</span><span><CheckIcon /> Orçamentos em poucos minutos</span><span><CheckIcon /> Histórico centralizado</span></div></div><div className="auth-form-wrap">{sent ? <div className="success-state"><span className="success-icon"><CheckIcon /></span><h2>Cadastro recebido.</h2><p>Nossa equipe vai validar os dados comerciais. Você receberá a confirmação de acesso no e-mail informado.</p><Link href="/catalogo" className="button button--dark">Voltar ao catálogo</Link></div> : <form className="signup-form" onSubmit={submit}><div><p className="section-note">CADASTRO B2B</p><h2>Dados da empresa</h2><p>Leva cerca de 3 minutos.</p></div><div className="form-grid form-grid--2"><div className="field"><label htmlFor="razao">Razão social</label><input id="razao" name="razaoSocial" required autoComplete="organization" /></div><div className="field"><label htmlFor="cnpj">CNPJ</label><input id="cnpj" name="cnpj" placeholder="00.000.000/0000-00" required inputMode="numeric" /></div></div><div className="form-grid form-grid--2"><div className="field"><label htmlFor="segmento">Segmento</label><select id="segmento" name="segmento" required defaultValue=""><option value="" disabled>Selecione</option><option>Papelaria</option><option>Loja de presentes</option><option>Atacado / distribuição</option><option>Outro varejo</option></select></div><div className="field"><label htmlFor="cidade">Cidade / UF</label><input id="cidade" name="cidade" required /></div></div><hr /><div><h3>Responsável pelas compras</h3></div><div className="form-grid form-grid--2"><div className="field"><label htmlFor="responsavel">Nome</label><input id="responsavel" name="responsavel" required autoComplete="name" /></div><div className="field"><label htmlFor="cargo">Cargo</label><input id="cargo" name="cargo" required /></div></div><div className="form-grid form-grid--2"><div className="field"><label htmlFor="email-cadastro">E-mail profissional</label><input id="email-cadastro" name="email" type="email" required autoComplete="email" /></div><div className="field"><label htmlFor="telefone">WhatsApp</label><input id="telefone" name="telefone" type="tel" required autoComplete="tel" /></div></div><label className="checkbox"><input name="consentimento" value="aceito" type="checkbox" required /><span>Confirmo que represento a empresa informada e aceito o tratamento dos dados para atendimento comercial.</span></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button button--dark button--full" type="submit" disabled={loading}>{loading?'Enviando…':'Enviar cadastro com segurança'} <LockIcon /></button><p className="form-footnote">Já é parceiro? <Link href="/orcamento">Acessar orçamento</Link></p></form>}</div></section>;
}
